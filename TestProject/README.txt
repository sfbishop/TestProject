A sample dynamic web project that contains the following:
build.xml with JUnit Test Reports
build date and build version will be automatically updated each time the ant script is run
Project is deployed on Jenkins